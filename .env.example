GROQ_API_KEY=your_key_here

# Unpaywall: finds legal open-access PDFs for papers by DOI (author pages, repositories, PMC…)
# Free — just provide any valid email address per Unpaywall's terms of service.
# Without this, only PDFs already known to Semantic Scholar will be fetched.
UNPAYWALL_EMAIL=you@example.com

# Optional: Semantic Scholar API key (raises rate limit from 1 req/s to 10 req/s)
# Get one at: https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=

# Optional: NCBI API key (raises PubMed rate limit from 3 req/s to 10 req/s)
# Get one at: https://www.ncbi.nlm.nih.gov/account/
NCBI_API_KEY=
